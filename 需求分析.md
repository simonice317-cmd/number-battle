# 数字对战 — 需求分析文档

> 版本：1.0  
> 最后更新：2026-08-08  
> 项目地址：https://simonice317-cmd.github.io/number-battle/

---

## 1. 项目概述

### 1.1 项目背景

"数字对战"是一款面向移动端和桌面端的双人在线策略对战游戏。玩家通过数字运算改变场上数值，触发角色技能攻击对手，将对方 HP 降至 0 即可获胜。项目采用纯前端技术栈，通过 WebRTC 实现 P2P 联机，部署于 GitHub Pages。

### 1.2 核心玩法

| 要素 | 描述 |
|------|------|
| 游戏类型 | 回合制策略对战 |
| 玩家数量 | 2 人 |
| 对战模式 | 本地同屏 / P2P 在线联机 |
| 单局时长 | 约 3-8 分钟 |
| 目标平台 | 移动端优先，兼容桌面端 |

### 1.3 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端框架 | 原生 JavaScript (ES Modules) | 无框架依赖，零构建步骤 |
| 样式 | 原生 CSS + CSS 自定义属性 | 玻璃拟态风格 |
| P2P 通信 | PeerJS (WebRTC) | 信令服务器 `0.peerjs.com:443` |
| 部署 | GitHub Pages | 免费静态托管 |
| 版本控制 | Git + GitHub | HTTPS 推送 |

---

## 2. 功能需求

### 2.1 大厅系统

#### 2.1.1 昵称设置
- **描述**：玩家在对战开始前输入昵称（最长 8 个字符）
- **优先级**：P0（必须）
- **验收标准**：输入昵称后方可进入对战；空昵称时按钮不可用

#### 2.1.2 本地对战
- **描述**：两名玩家在同一设备上轮流操作
- **优先级**：P0
- **验收标准**：点击"本地对战"后直接进入角色选择，双方轮流操作同一屏幕

#### 2.1.3 创建房间（P2P 房主）
- **描述**：生成 4 位房间码，等待对手加入
- **优先级**：P0
- **验收标准**：
  - 房间码 4 位数字，显示在等待页
  - 对手输入正确房间码后自动建立 P2P 连接
  - 连接失败时显示错误提示

#### 2.1.4 加入房间（P2P 客机）
- **描述**：输入 4 位房间码加入已有房间
- **优先级**：P0
- **验收标准**：
  - 输入 4 位数字后自动连接
  - 房间不存在或满员时显示提示
  - 连接成功后进入游戏流程

---

### 2.2 硬币猜测（先手决定）

#### 2.2.1 猜硬币机制
- **描述**：房主抛硬币（随机正/反面），客机猜测结果，猜对者先手
- **优先级**：P0
- **验收标准**：
  - 硬币显示翻转动画
  - 结果展示猜对/猜错及先手归属
  - 本地模式随机分配先手

---

### 2.3 角色选择

#### 2.3.1 角色展示
- **描述**：展示所有可选角色的头像、名称、HP、技能列表和组合技信息
- **优先级**：P0
- **验收标准**：
  - 卡片式展示，可点击选中
  - 技能用"数字 → 效果描述"格式展示
  - 有组合技的角色显示组合技条件
  - 确认前可切换选择

#### 2.3.2 角色锁定
- **描述**：双方选定角色后进入对战
- **优先级**：P0
- **验收标准**：
  - P2P 模式：双方确认后开始
  - 本地模式：双方依次选择

#### 2.3.3 现有角色清单

| ID | 名称 | HP | 技能 | 组合技 |
|----|------|-----|------|--------|
| basic | 基础使者 | 4 | 4→冲拳(伤1), 5→护盾(+1), 9→回血(+1) | 无 |
| paladin | 圣骑士 | 5 | 4→冲拳(伤1), 5→盾击(盾+伤1), 6→号角(攻+1), 9→回血(+1) | 无 |
| archer | 弓箭手 | 3 | 3→速射(伤1), 5→轻甲(+1盾), 9→回血(+1) | 3+6→万箭齐发(伤2+回1) |
| thief | 盗贼 | 4 | 1→偷数字, 4→刺击(伤1), 6→妙手(偷资源), 7→重击(穿盾) | 无 |

---

### 2.4 对战核心

#### 2.4.1 回合制框架
- **描述**：双方轮流操作，每回合最多 2 次操作，其中加法最多 1 次
- **优先级**：P0
- **验收标准**：
  - 当前回合方高亮显示
  - 操作次数实时显示 "操作 X/2 | 加法 X/1"
  - 操作用完或手动跳过自动切换回合
  - 30 秒倒计时，超时判负
  - 非己方回合时操作按钮全部隐藏

#### 2.4.2 加法操作
- **描述**：我方的数字 A 与对手的数字 B 相加，个位数结果覆盖我方 A
- **交互方式**：拖拽我方数字牌到对方数字牌
- **优先级**：P0
- **验收标准**：
  - 公式：`A_new = (A + B) % 10`
  - 加法后自动重算技能就绪状态
  - 加法后自动解锁组合技（若已锁定）
  - 每回合只能做 1 次加法
  - 两数相同（如 5+5）时，任意选择均可

#### 2.4.3 技能系统
- **描述**：数字牌的值匹配角色技能表时，该数字变为技能就绪状态（显示 ✨），可拖拽释放
- **优先级**：P0
- **拖拽目标判定**：

| 拖到 | 说明 | 示例 |
|------|------|------|
| 对方数字牌 | 加法（默认）或偷取数字（盗贼 1） | — |
| 对方头像 | 攻击类技能（伤害/偷资源/穿甲） | 冲拳、重击 |
| 自己头像 | 防御/回复类技能（护盾/回血/Buff） | 圣泉、号角 |

- **技能类型**：

| 类型 | 效果 | 使用者 |
|------|------|--------|
| damage | 造成 X 点伤害（护盾优先吸收） | 基础使者、圣骑士、弓箭手、盗贼 |
| shield | 自身获得 X 点护盾 | 基础使者、弓箭手 |
| shield_strike | 自身+护盾并造成伤害 | 圣骑士 |
| heal | 回复 X 点 HP（不超过上限） | 基础使者、圣骑士、弓箭手 |
| buff | 下次攻击伤害 +X（上限 1） | 圣骑士 |
| pierce_damage | 无视护盾直接造成 X 点伤害 | 盗贼 |
| steal_number | 复制对手一个数字的值 | 盗贼 |
| steal_resource | 偷取对手 1 点护盾或血量（优先护盾） | 盗贼 |

- **验收标准**：
  - 技能就绪数字显示 ✨ 标记
  - 拖拽到错误目标时拒绝并提示
  - 技能使用后 ✨ 消失
  - 伤害先扣护盾再扣血
  - 生命回复不超过最大 HP

#### 2.4.4 组合技系统
- **描述**：场上同时存在指定的数字组合时，可点击按钮释放组合技
- **优先级**：P1
- **验收标准**：
  - 条件满足时显示 💥 按钮
  - 使用后锁定（按钮消失），需做一次加法解锁
  - 技能弹窗中显示组合技信息
  - 角色选择卡片显示组合技条件

#### 2.4.5 伤害结算流程
- **描述**：规范化伤害计算
- **优先级**：P0
- **结算流程**：
  1. 检查攻击方伤害加成（damageBuff），有则加上并清零
  2. 目标护盾 > 0 → 护盾先吸收伤害
  3. 剩余伤害扣减 HP
  4. HP ≤ 0 → 游戏结束，对手获胜

---

### 2.5 技能查看弹窗

#### 2.5.1 技能信息查看
- **描述**：对战中可随时查看当前角色的技能和组合技信息
- **优先级**：P1
- **验收标准**：
  - 点击 📋 按钮弹出浮层
  - 显示角色头像、名称、全部技能（数字+描述）
  - 有组合技的角色显示组合技信息
  - 点击背景或关闭按钮关闭弹窗
  - 仅在己方回合显示按钮

---

### 2.6 结算系统

#### 2.6.1 胜负判定
- **描述**：HP 归零、主动投降、超时均导致判负
- **优先级**：P0
- **验收标准**：
  - HP 归零 → 对手获胜
  - 点击投降 → 对手获胜
  - 回合超时 → 对手获胜
  - 结算页显示胜负图标和原因

#### 2.6.2 再来一局 / 返回
- **描述**：结算后可选重赛或返回大厅
- **优先级**：P0
- **验收标准**：
  - P2P 模式：双方确认后重赛（角色保持不变）
  - 返回大厅：断开连接回到首页

---

### 2.7 视觉特效

#### 2.7.1 技能特效
- **描述**：操作触发即时视觉反馈
- **优先级**：P2
- **验收标准**：
  - 伤害 → 红色闪烁 + "-N" 浮动文字
  - 治疗 → 绿色闪烁 + "+N❤️" 浮动文字
  - 护盾 → 青色闪烁 + "+N🛡️" 浮动文字
  - Buff → 黄色闪烁 + "⚔+N" 浮动文字
  - 穿甲 → 深红闪烁 + "💀-N" 浮动文字
  - 偷取 → 紫色闪烁 + 对应浮动文字

#### 2.7.2 组合技特效
- **描述**：组合技触发双段动画
- **优先级**：P2
- **验收标准**：对手头像受击闪 + 我方头像治疗闪，间隔 300ms

#### 2.7.3 Toast 提示
- **描述**：操作结果文字提示
- **优先级**：P2
- **验收标准**：屏幕中下方显示，2 秒自动消失

---

## 3. 非功能需求

### 3.1 性能

| 指标 | 要求 |
|------|------|
| 首屏加载 | < 3 秒（含 PeerJS 库） |
| 操作响应 | < 100ms（本地），< 300ms（P2P） |
| 内存占用 | < 50MB |
| 包体积 | 所有 JS/CSS < 100KB（压缩前） |

### 3.2 兼容性

| 平台 | 最低版本 |
|------|----------|
| Chrome | 90+ |
| Safari | 15+ (iOS) |
| Firefox | 90+ |
| Edge | 90+ |
| 微信内置浏览器 | iOS 14+ / Android 10+ |

### 3.3 可用性

- 移动端触控友好：拖拽适配 touch 事件
- 按钮最小点击区域 44×44px（符合 iOS HIG）
- 色彩对比度满足 WCAG AA 标准
- P2P 连接失败有明确提示和重试引导

### 3.4 安全性

- 无服务端存储，不收集用户隐私数据
- P2P 通信仅传输游戏状态，无个人信息泄露风险
- 房主验证操作合法性，客机仅转发（防作弊基础保障）
- 无 API Key / Secret 硬编码

### 3.5 可维护性

- 角色注册表驱动：新增角色只需在 `CHARACTERS` 对象中添加一条配置
- 核心逻辑与 UI 分离：`game-core.js`（纯逻辑）+ `ui-render.js`（渲染）+ `main.js`（交互）
- 提取共享工具函数消除重复（减少 328 行冗余）
- CSS 自定义属性实现动画复用（6 个 keyframes → 1 个）

---

## 4. 架构设计

### 4.1 文件结构

```
youxi/
├── index.html          # 入口 HTML（全屏布局）
├── css/
│   └── style.css       # 全局样式（玻璃拟态风格）
├── js/
│   ├── game-core.js    # 纯逻辑层：角色注册表、操作函数、胜负判定
│   ├── ui-render.js    # 渲染层：DOM 更新、技能弹窗、特效
│   ├── main.js         # 应用入口：状态管理、P2P 通信、事件绑定
│   ├── drag-handler.js # 拖拽交互：touch/mouse 事件处理
│   └── peerjs.min.js   # PeerJS 库（本地打包）
└── 需求分析.md          # 本文档
```

### 4.2 模块职责

```
┌─────────────────────────────────────────┐
│                  index.html              │
│           (全屏页面 + DOM 结构)           │
└─────────────────────────────────────────┘
                    ↑
┌─────────────────────────────────────────┐
│                main.js                   │
│  状态管理 · P2P通信 · 事件绑定 · 流程控制  │
├─────────────────────────────────────────┤
│          ui-render.js                    │
│   DOM 同步 · 特效动画 · Toast · 弹窗      │
├─────────────────────────────────────────┤
│          game-core.js                    │
│  角色注册表 · 操作逻辑 · 校验 · 胜负判定   │
│  （纯函数，不依赖 DOM）                   │
└─────────────────────────────────────────┘
```

### 4.3 P2P 通信流程

```
房主 (Host)                      客机 (Guest)
    │                                │
    ├─ 创建房间，显示房间码 ──────────┤
    │                                ├─ 输入房间码，建立连接
    │                                │
    ├─ 抛硬币 ──── result ──────────→│
    │←── guess ──── 猜测 ───────────┤
    ├─ 广播结果 ── coin_result ─────→│
    │                                │
    ├─ 选角色 ─── char_select ──────→│
    │←─ char_select ─ 选角色 ───────┤
    │                                │
    │  ╔═══ 对战循环 ═══╗            │
    ├─ 操作 ──── action ───────────→│ (客机回合)
    │←── state_update ─ 广播状态 ───┤ (房主验证)
    │                                │
    ├─ state_update ───────────────→│ (房主回合)
    │  ╚══════════════════╝            │
    │                                │
    ├─ 结算 ─── game_over ──────────→│
```

### 4.4 数据流

```
用户操作 (拖拽/点击)
    │
    ▼
resolveDragAction()     ← 判定操作类型（加法/技能）
    │
    ▼
doAdd() / useSkill() / useCombo()   ← game-core.js 纯逻辑
    │
    ▼
applyActionResult()     ← main.js：更新状态 → 渲染 → toast → 胜利检查
    │
    ├─ 本地模式：直接更新 DOM
    └─ P2P 模式：广播 state_update 给对手
```

---

## 5. 未来扩展方向

### 5.1 高优先级
- [ ] **更多角色**：利用角色注册表机制，新增角色仅需配置
- [ ] **更多组合技**：为现有角色（圣骑士、盗贼）增加组合技
- [ ] **音效系统**：操作音效 + 背景音乐
- [ ] **断线重连**：P2P 断线后自动尝试重连

### 5.2 中优先级
- [ ] **观战模式**：第三人观看对局
- [ ] **战绩记录**：localStorage 保存胜负统计
- [ ] **AI 对手**：单人 vs 电脑（基础难度）
- [ ] **房间密码**：可选密码保护私人房间

### 5.3 低优先级
- [ ] **多语言支持**：英文 / 日文
- [ ] **自定义角色**：玩家自定义技能组合
- [ ] **排行榜**：需要后端服务
- [ ] **锦标赛模式**：多轮淘汰制

---

## 6. 已知限制

| 限制 | 说明 | 影响 |
|------|------|------|
| P2P NAT 穿透 | 对称 NAT 网络可能无法连接 | 约 5-10% 用户需切换网络 |
| 无服务端 | 无法持久化战绩、无法反外挂 | 仅适合休闲对战 |
| 单局 2 人 | 不支持 3+ 多人混战 | — |
| GitHub Pages | 静态托管，不支持 SSR | 首次加载略慢 |
| 信令依赖 | 依赖 `0.peerjs.com` 公共信令服务 | 该服务不可用时无法联机 |

---

## 7. 术语表

| 术语 | 说明 |
|------|------|
| 数字牌 | 场上 4 张带数值的卡片（双方各 2 张） |
| 技能就绪 (skillReady) | 数字值匹配角色技能表，可释放技能的状态 |
| 组合技锁定 (comboUsed) | 组合技使用后进入锁定，需做加法解锁 |
| 伤害加成 (damageBuff) | 圣骑士号角效果，下次攻击伤害 +1 |
| 穿甲伤害 | 无视护盾直接扣血的伤害类型 |
| 信令服务 | WebRTC 建立 P2P 连接前交换网络信息的中转服务 |
